@inherits LayoutComponentBase

@inject AuthenticationStateProvider AuthenticationStateProvider

@inject NavigationManager NavigationManager

@inject IJSRuntime JSRuntime

<MudThemeProvider />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar>
        <MudButtonGroup Style="color: white;" Variant="Variant.Text">
            <MudButton Href="orders" Edge="Edge.Start" Color="Color.Inherit">Orders</MudButton>
            <MudButton Href="customers" Edge="Edge.Start" Color="Color.Inherit">Customers</MudButton>
        </MudButtonGroup>

        <MudSpacer/>

        <!--

        @if (!IsUserLoggedIn)
        {
            <MudButtonGroup Color="Color.Primary" Variant="Variant.Text">
                <MudButton Color="Color.Inherit" Href="/Account/Login">Log In</MudButton>
                <MudButton Color="Color.Inherit" Href="/Account/Register">Register</MudButton>
            </MudButtonGroup>
        }
        else
        {
            <MudButtonGroup Color="Color.Primary" Variant="Variant.Text">
                <MudButton Color="Color.Inherit" Href="/Account/Login">LogOut</MudButton>
            </MudButtonGroup>
        }

        -->

    </MudAppBar>

    <MudMainContent>
        @Body
    </MudMainContent>

</MudLayout>
@code {
     /*
       private bool IsUserLoggedIn = false;

       protected override async Task OnInitializedAsync()
       {
       var authState = await AuthenticationStateProvider.GetAuthenticationStateAsync();

       if (authState.User.Identity == null)
           IsUserLoggedIn = false;
       else
           IsUserLoggedIn = authState.User.Identity.IsAuthenticated;
       }
       */

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            try
            {
                // Ensure JSInterop is only called after the component is interactive
                await JSRuntime.InvokeVoidAsync("mudBlazorInit");
            }
            catch (Exception ex)
            {
                Console.WriteLine($"Error during JS initialization: {ex.Message}");
            }
        }
    }
}